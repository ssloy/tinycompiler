main() {
    int main(int x) {
        return x;
    }

    int main(int x, int y) {
        return x + y;
    }

    int main(bool x) {
        if x {
            return 0;
        }
        return 2;
    }

    println main(0);
    println main(0, 1);
    println main(false);
}

