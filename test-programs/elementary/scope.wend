main() {
    int x;

    h() {
//      int x;
        f(0);
    }

    f(int y) {
        g(int y) {
            x = y + 1;
        }

        x = y;
        g(x + 1);
    }

    x = 0;
    f(x + 1);
    println x;
    h();
    println x;
}

